/**
 * HTTP API endpoints.
 */

var express = require('express');
var router = express.Router();

// mounting donor API
// var donorApi = require('./donors');
// router.use('/donors', donorApi);

module.exports = router;
